#include <cassert>

#include "bitstream_format.h"

using namespace std;
using namespace TinyPnR;

int main(const int argc, const char** argv) {
  assert(argc == 5);

  string app_file = argv[1];
  string bitstream_format_file = argv[2];
  string topology_file = argv[3];
  string application_file = argv[4];

  
}
